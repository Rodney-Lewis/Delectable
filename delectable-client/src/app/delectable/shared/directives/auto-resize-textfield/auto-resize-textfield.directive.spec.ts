import { AutoResizeTextfieldDirective } from './auto-resize-textfield.directive';

describe('AutoResizeTextfieldDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoResizeTextfieldDirective();
    expect(directive).toBeTruthy();
  });
});
