import { CalendarMultiDateSelectDirective } from './calendar-multi-date-select.directive';

describe('CalendarMultiDateSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarMultiDateSelectDirective();
    expect(directive).toBeTruthy();
  });
});
