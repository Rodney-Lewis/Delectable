import { AutoRemoveDirectionDirective } from './auto-remove-direction.directive';

describe('AutoRemoveDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoRemoveDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
