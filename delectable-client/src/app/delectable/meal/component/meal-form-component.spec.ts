import { MealFormComponent } from './meal-form-component';

describe('MealFormComponent', () => {
  it('should create an instance', () => {
    expect(new MealFormComponent()).toBeTruthy();
  });
});
