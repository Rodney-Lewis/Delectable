import { PantryDirective } from './pantry.directive';

describe('PantryDirective', () => {
  it('should create an instance', () => {
    const directive = new PantryDirective();
    expect(directive).toBeTruthy();
  });
});
