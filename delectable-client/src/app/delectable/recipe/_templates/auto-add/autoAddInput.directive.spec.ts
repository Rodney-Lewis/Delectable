import { AutoAddInput } from './autoAddInput';

describe('AddingredientDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoAddInput();
    expect(directive).toBeTruthy();
  });
});
