import { AutoRemoveInput } from './autoRemoveInput';

describe('RemoveingredientDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoRemoveInput();
    expect(directive).toBeTruthy();
  });
});
