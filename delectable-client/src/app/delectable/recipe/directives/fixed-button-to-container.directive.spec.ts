import { FixedButtonToContainerDirective } from './fixed-button-to-container.directive';

describe('FixedButtonToContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedButtonToContainerDirective();
    expect(directive).toBeTruthy();
  });
});
