import { AutoAddDirectionDirective } from './auto-add-direction.directive';

describe('AutoAddDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoAddDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
