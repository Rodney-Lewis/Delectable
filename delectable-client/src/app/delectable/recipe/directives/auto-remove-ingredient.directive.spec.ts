import { AutoRemoveIngredientDirective } from './auto-remove-ingredient.directive';

describe('AutoRemoveIngredientDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoRemoveIngredientDirective();
    expect(directive).toBeTruthy();
  });
});
