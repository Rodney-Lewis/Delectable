import { AutoAddIngredientDirective } from './auto-add-ingredient.directive';

describe('AutoAddIngredientDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoAddIngredientDirective();
    expect(directive).toBeTruthy();
  });
});
